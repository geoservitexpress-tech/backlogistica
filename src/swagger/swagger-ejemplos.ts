/** Credenciales y referencias de prueba para Swagger (`/docs`). */
export const SWAGGER_EJEMPLO_CORREO = 'juan@gmail.com';
export const SWAGGER_EJEMPLO_PASSWORD = '12345678';

/** `usuarios.id_usuario` del primer registro (entero). Tras **POST /auth/register** use **GET /auth/me**. */
export const SWAGGER_EJEMPLO_ID_USUARIO = 1;

/** `pedidos.id_pedido` de ejemplo (entero tras seed / primer pedido creado). */
export const SWAGGER_EJEMPLO_ID_PEDIDO = 1;
